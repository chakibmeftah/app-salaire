- name: "installation et déploiement des rôles"
  hosts: all
  roles:
    - mariadb
    - apache

  hosts: myHost
  roles:
    - edit_hosts_file
  
  hosts: localhost
  vars_files:
  - credentials_info.yml
  roles:
    - ansible_sshpass
  
  